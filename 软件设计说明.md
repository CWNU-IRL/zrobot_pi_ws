# ZRobot Pi 人形机器人运动控制系统 软件设计说明

> **版本**：1.0  
> **适用平台**：Raspberry Pi / Linux + ROS 2 (Humble/Jazzy)  
> **编写单位**：CWNU-IRL

---

## 目录

1. [引言](#1-引言)
2. [总体设计](#2-总体设计)
   - 2.1 [软件体系结构](#21-软件体系结构)
   - 2.2 [软件结构图](#22-软件结构图)
   - 2.3 [模块划分](#23-模块划分)
   - 2.4 [运行环境](#24-运行环境)
3. [接口设计](#3-接口设计)
   - 3.1 [ROS 2 话题接口](#31-ros-2-话题接口)
   - 3.2 [ROS 2 服务接口](#32-ros-2-服务接口)
   - 3.3 [自定义消息/服务定义](#33-自定义消息服务定义)
   - 3.4 [硬件接口](#34-硬件接口)
4. [模块详细设计](#4-模块详细设计)
   - 4.1 [imu_msg — IMU 消息定义包](#41-imu_msg--imu-消息定义包)
   - 4.2 [rs_interface — 电机服务接口定义包](#42-rs_interface--电机服务接口定义包)
   - 4.3 [imu_data_node — IMU 驱动节点](#43-imu_data_node--imu-驱动节点)
   - 4.4 [zrobot_bridge — 电机控制桥接节点](#44-zrobot_bridge--电机控制桥接节点)
   - 4.5 [zrobot_control — 电机测试客户端节点](#45-zrobot_control--电机测试客户端节点)
   - 4.6 [zrobot_deploy — 部署与 FSM 控制节点](#46-zrobot_deploy--部署与-fsm-控制节点)
5. [函数名称与功能说明](#5-函数名称与功能说明)
   - 5.1 [ImuDriverNode 函数列表](#51-imudrivernode-函数列表)
   - 5.2 [RobStrideMotor 函数列表](#52-robstridemotor-函数列表)
   - 5.3 [MotorControllerNode 函数列表](#53-motorcontrollernode-函数列表)
   - 5.4 [FSM 基类函数列表](#54-fsm-基类函数列表)
   - 5.5 [FixStand 函数列表](#55-fixstand-函数列表)
   - 5.6 [Locomotion 函数列表](#56-locomotion-函数列表)
6. [算法设计](#6-算法设计)
   - 6.1 [IMU 数据解析算法](#61-imu-数据解析算法)
   - 6.2 [欧拉角转四元数算法](#62-欧拉角转四元数算法)
   - 6.3 [CAN 扩展帧 ID 编解码算法](#63-can-扩展帧-id-编解码算法)
   - 6.4 [浮点数与 16 位定点数互转算法](#64-浮点数与-16-位定点数互转算法)
   - 6.5 [线性插值（Lerp）站立算法](#65-线性插值lerp站立算法)
   - 6.6 [强化学习推理（Locomotion）算法](#66-强化学习推理locomotion算法)
7. [各功能流程图](#7-各功能流程图)
   - 7.1 [系统启动流程](#71-系统启动流程)
   - 7.2 [IMU 数据采集与发布流程](#72-imu-数据采集与发布流程)
   - 7.3 [电机运控服务处理流程](#73-电机运控服务处理流程)
   - 7.4 [电机零位标定流程](#74-电机零位标定流程)
   - 7.5 [FixStand 状态机流程](#75-fixstand-状态机流程)
   - 7.6 [Locomotion 推理控制流程](#76-locomotion-推理控制流程)
   - 7.7 [FSM 主程序交互流程](#77-fsm-主程序交互流程)
8. [逻辑框图](#8-逻辑框图)
   - 8.1 [整体逻辑框图](#81-整体逻辑框图)
   - 8.2 [IMU 驱动逻辑框图](#82-imu-驱动逻辑框图)
   - 8.3 [电机控制逻辑框图](#83-电机控制逻辑框图)
   - 8.4 [FSM 状态转换逻辑框图](#84-fsm-状态转换逻辑框图)
   - 8.5 [Locomotion 强化学习控制逻辑框图](#85-locomotion-强化学习控制逻辑框图)
9. [运行设计](#9-运行设计)
   - 9.1 [节点启动顺序](#91-节点启动顺序)
   - 9.2 [控制频率与实时性](#92-控制频率与实时性)
   - 9.3 [线程模型](#93-线程模型)
   - 9.4 [错误处理与容错机制](#94-错误处理与容错机制)
   - 9.5 [参数配置说明](#95-参数配置说明)
10. [附录](#10-附录)
    - 10.1 [电机类型参数表](#101-电机类型参数表)
    - 10.2 [CAN 通信类型定义表](#102-can-通信类型定义表)
    - 10.3 [电机配置映射表](#103-电机配置映射表)

---

## 1. 引言

### 1.1 编写目的

本文档为 ZRobot Pi 人形机器人运动控制系统的软件设计说明，供系统开发、测试、维护人员参考。文档描述了系统的总体软件架构、各模块的功能与接口、核心算法及运行设计。

### 1.2 项目概述

ZRobot Pi 是基于 ROS 2 框架开发的人形机器人嵌入式控制系统，运行于 Raspberry Pi 等 Linux 平台。系统通过 CAN 总线驱动 23 个 RobStride 系列关节电机，通过串口读取 IMU 惯性测量单元数据，并在有限状态机（FSM）框架下实现：

- **站立保持（FixStand）**：将所有关节电机从当前位置插值过渡到机械零位；
- **运动控制（Locomotion）**：加载经强化学习训练的 ONNX 策略模型，以 100 Hz 频率实时推理并输出 12 维关节位置指令。

### 1.3 术语与缩略语

| 缩略语 | 全称 / 含义 |
|--------|-------------|
| ROS 2 | Robot Operating System 2，机器人操作系统第二代 |
| IMU | Inertial Measurement Unit，惯性测量单元 |
| CAN | Controller Area Network，控制器局域网 |
| FSM | Finite State Machine，有限状态机 |
| ONNX | Open Neural Network Exchange，开放神经网络交换格式 |
| PD | Proportional-Derivative，比例-微分控制 |
| TTL | Transistor-Transistor Logic，TTL 串口协议 |
| RS485 | 差分串口通信协议 |

---

## 2. 总体设计

### 2.1 软件体系结构

系统采用**分层式**软件体系结构，共分为三个层次：

```
┌─────────────────────────────────────────────────────────────────┐
│                      决策控制层                                  │
│   zrobot_deploy（FSM 状态机 + FixStand + Locomotion）            │
│   zrobot_control（测试/调试客户端）                               │
└────────────────────────┬────────────────────────────────────────┘
                         │ ROS 2 Service 调用
┌────────────────────────▼────────────────────────────────────────┐
│                      驱动适配层                                  │
│   zrobot_bridge（MotorControllerNode + RobStrideMotor）          │
│   imu_data_node（ImuDriverNode）                                 │
└────────────────────────┬────────────────────────────────────────┘
                         │ 硬件通信
┌────────────────────────▼────────────────────────────────────────┐
│                      硬件层                                      │
│   23 × RobStride 电机（CAN 总线 can10/can11/can12/can13）        │
│   IMU 传感器（串口 /dev/imu_usb）                                │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 软件结构图

```
zrobot_pi_ws/src/
├── imu_msg/                        ← 消息定义包
│   └── msg/ImuData.msg             ← 含欧拉角的 IMU 自定义消息
│
├── rs_interface/                   ← 服务接口定义包
│   └── srv/
│       ├── RobStrideMsgs.srv       ← 23 电机运控服务
│       ├── SetZeros.srv            ← 零位标定服务
│       └── GetPositions.srv        ← 获取当前位置服务
│
├── imu_data_node/                  ← IMU 驱动节点包
│   ├── include/imu_data_node/
│   │   └── imu_driver_node.hpp     ← ImuDriverNode 类声明
│   └── src/
│       └── imu_driver_node.cpp     ← 串口读取、协议解析、ROS 发布
│
├── zrobot_bridge/                  ← 电机控制桥接包
│   ├── include/zrobot_bridge/
│   │   ├── motor_cfg.h             ← RobStrideMotor 类声明 + 数据结构
│   │   └── motor_controller.h     ← MotorControllerNode 类声明
│   ├── src/
│   │   ├── motor_cfg.cpp           ← 单电机 CAN 通信实现
│   │   └── motor_controller_node.cpp ← ROS 服务服务端实现
│   ├── config/
│   │   └── motor_config.yaml       ← 23 电机 CAN ID / 类型 / 接口配置
│   └── launch/
│       └── motor_controller.launch.py ← 启动文件
│
├── zrobot_control/                 ← 测试客户端包
│   └── src/
│       └── rob_stride_client_node.cpp ← 定时发送测试位置指令
│
└── zrobot_deploy/                  ← 部署 & FSM 控制包
    ├── include/zrobot_deploy/
    │   ├── FSM.h                   ← FSM 抽象基类
    │   ├── FixStand.h              ← 站立状态机声明
    │   └── Locomotion.h            ← 行走状态机（ONNX 推理）声明
    └── src/
        ├── main.cpp                ← 交互式入口，键盘切换状态机
        ├── FSM.cpp                 ← FSM 基类实现（服务调用封装）
        ├── FixStand.cpp            ← 站立插值逻辑实现
        └── Locomotion.cpp          ← ONNX 推理线程实现
```

### 2.3 模块划分

| 包名 | 可执行文件 | 类型 | 主要功能 |
|------|-----------|------|---------|
| `imu_msg` | — | 消息定义 | 定义含欧拉角的 IMU 自定义消息类型 |
| `rs_interface` | — | 服务定义 | 定义电机运控、零位、位置查询服务类型 |
| `imu_data_node` | `imu_data_node` | 传感器驱动节点 | 读取 IMU 串口数据，发布 ROS 2 话题 |
| `zrobot_bridge` | `motor_controller_node` | 执行器驱动节点 | 管理 23 个 CAN 电机，提供 ROS 2 服务 |
| `zrobot_control` | `rob_stride_client_node` | 测试/调试节点 | 定时调用电机服务，验证通信链路 |
| `zrobot_deploy` | `zrobot_deploy_main` | 控制决策节点 | FSM 框架，支持站立与强化学习行走 |

### 2.4 运行环境

| 项目 | 要求 |
|------|------|
| 操作系统 | Ubuntu 22.04 LTS（ARM64）|
| ROS 版本 | ROS 2 Humble Hawksbill |
| 编译器 | GCC 11+ / Clang 14+（C++17）|
| 构建工具 | colcon + ament_cmake |
| 外部依赖库 | libserial, Eigen3, ONNX Runtime, tf2 |
| 硬件接口 | 4 路 CAN 总线（can10/11/12/13），1 路 UART 串口 |
| 电机 | 23 × RobStride 系列（00/02/03/04 型）|
| IMU | 串口 IMU，支持 TTL / CAN / RS485 协议 |

---

## 3. 接口设计

### 3.1 ROS 2 话题接口

| 话题名 | 消息类型 | 方向 | 发布节点 | 订阅节点 | 频率 | 描述 |
|--------|---------|------|---------|---------|------|------|
| `imu/data` | `sensor_msgs/Imu` | 发布 | `imu_data_node` | `Locomotion` | ~50 Hz | 标准 IMU 数据（角速度、加速度、四元数）|
| `imu/ImuDataWithRPY` | `imu_msg/ImuData` | 发布 | `imu_data_node` | — | ~50 Hz | 附加欧拉角的扩展 IMU 数据 |
| `cmd_vel` | `geometry_msgs/Twist` | 订阅 | 外部速度指令源 | `Locomotion` | 按需 | 线速度与角速度指令（vx, vy, wz）|

### 3.2 ROS 2 服务接口

| 服务名 | 服务类型 | 服务端节点 | 调用方节点 | 功能 |
|--------|---------|-----------|---------|------|
| `/motor_controller_node/rob_stride_control` | `rs_interface/RobStrideMsgs` | `motor_controller_node` | `FSM` 子类 / `rob_stride_client_node` | 发送 23 个目标位置，返回全部反馈 |
| `/motor_controller_node/set_zeros` | `rs_interface/SetZeros` | `motor_controller_node` | `FSM` 子类 | 将所有电机当前位置设为机械零位 |
| `/motor_controller_node/get_positions` | `rs_interface/GetPositions` | `motor_controller_node` | `FSM` 子类 | 查询 23 个电机的当前位置 |

### 3.3 自定义消息/服务定义

#### `imu_msg/ImuData.msg`

```
std_msgs/Header header        # 时间戳和坐标系 ID
sensor_msgs/Imu imu           # 标准 IMU 数据
float64 roll                  # 绕 X 轴欧拉角（rad）
float64 pitch                 # 绕 Y 轴欧拉角（rad）
float64 yaw                   # 绕 Z 轴欧拉角（rad）
```

#### `rs_interface/RobStrideMsgs.srv`

```
# ===== 请求 =====
float32[23] positions         # 23 个电机目标位置（rad）

---
# ===== 响应 =====
float32[23] feedback_positions     # 反馈位置（rad）
float32[23] feedback_velocities    # 反馈速度（rad/s）
float32[23] feedback_torques       # 反馈力矩（N·m）
float32[23] feedback_temperatures  # 反馈温度（°C）
bool success                       # 是否成功
string message                     # 附加信息
```

#### `rs_interface/SetZeros.srv`

```
# ===== 请求 =====
（无请求字段）

---
# ===== 响应 =====
bool success
string message
```

#### `rs_interface/GetPositions.srv`

```
# ===== 请求 =====
（无请求字段）

---
# ===== 响应 =====
float32[23] feedback_positions    # 当前 23 个电机位置（rad）
bool success
string message
```

### 3.4 硬件接口

#### CAN 总线接口

| 接口名 | 挂载电机索引 | 电机数量 | 描述 |
|--------|-------------|---------|------|
| `can10` | 0 – 5 | 6 | 左腿下肢电机 |
| `can11` | 6 – 12 | 7 | 右腿下肢电机 |
| `can12` | 13 – 16 | 4 | 左臂电机 |
| `can13` | 17 – 22 | 6 | 右臂 + 腰部电机 |

CAN 报文格式：**扩展帧（CAN_EFF）**，波特率 1 Mbps。

#### 串口接口

| 参数 | 默认值 |
|------|--------|
| 设备路径 | `/dev/imu_usb` |
| 波特率 | 2,000,000 bps |
| 数据位 | 8 |
| 停止位 | 1 |
| 校验位 | 无 |

---

## 4. 模块详细设计

### 4.1 `imu_msg` — IMU 消息定义包

- **类型**：纯消息定义包（`ament_cmake`）
- **功能**：定义 `ImuData` 消息类型，在标准 `sensor_msgs/Imu` 基础上增加 roll / pitch / yaw 欧拉角字段，方便上层模块直接使用姿态角而无需再次计算。
- **被依赖包**：`imu_data_node`

### 4.2 `rs_interface` — 电机服务接口定义包

- **类型**：纯服务定义包（`ament_cmake`）
- **功能**：定义三个 ROS 2 服务，供 `zrobot_bridge` 作为服务端、`zrobot_deploy` 作为客户端使用，实现电机控制命令的解耦传递。
- **被依赖包**：`zrobot_bridge`、`zrobot_deploy`、`zrobot_control`

### 4.3 `imu_data_node` — IMU 驱动节点

- **主类**：`imu_data_node::ImuDriverNode`（继承 `rclcpp::Node`）
- **职责**：打开串口、解析 IMU 数据帧、发布 ROS 2 话题
- **并发模型**：启动独立驱动线程 `driver_thread_`，主线程仅执行 ROS spin

**内部状态机**（协议解析）：

```
接收字节
   │
   ▼
[等待帧头 0x55]
   │ 匹配
   ▼
[读取类型字节]
   │
   ├── 0x51 → 加速度帧
   ├── 0x52 → 角速度帧
   ├── 0x53 → 姿态角帧
   └── 0x54 → 磁力计帧
   │
   ▼
[接收数据字节]
   │
   ▼
[校验和验证]
   │ 通过
   ▼
[processData() → publishData()]
```

**支持协议类型**：

| 协议 | 帧长度 | 数据字段数 | 说明 |
|------|--------|-----------|------|
| TTL | 11 字节 | 4 × int16 | 标准 UART 输出 |
| CAN | 8 字节 | 3 × int16 | CAN 总线输出 |
| RS485 | Modbus RTU | 3 × int16 | 主动查询模式 |

### 4.4 `zrobot_bridge` — 电机控制桥接节点

本包包含两个协作类：

#### 4.4.1 `RobStrideMotor`（`motor_cfg.h` / `motor_cfg.cpp`）

- **职责**：单个电机的 CAN 通信驱动，封装底层帧格式
- **通信方式**：Linux SocketCAN（`AF_CAN` / `SOCK_RAW`）
- **扩展 ID 格式**：

```
 bit[28:24] = communication_type（5位）
 bit[23:8]  = extra_data（16位）
 bit[7:0]   = motor_id（8位）
```

**支持的控制模式**：

| 控制模式 | 枚举值 | 描述 |
|---------|-------|------|
| 运控模式 | `0x01` | 同时指定位置/速度/力矩/Kp/Kd |
| 位置模式 PP | `0x01`（参数设置） | 位置轮廓控制 |
| 位置模式 CSP | `0x01`（参数设置） | 循环同步位置控制 |
| 速度模式 | `0x02` | 速度控制 |
| 电流模式 | `0x03` | 电流（力矩）直接控制 |
| 零位模式 | `0x06` | 设置机械零位 |

#### 4.4.2 `MotorControllerNode`（`motor_controller.h` / `motor_controller_node.cpp`）

- **职责**：管理 23 个 `RobStrideMotor` 对象，暴露 3 个 ROS 2 服务
- **并发安全**：使用 `std::mutex motors_mutex_` 保护电机数组
- **参数化配置**：启动时从 `motor_config.yaml` 读取所有电机的 CAN ID、电机类型、CAN 接口名称、Kp/Kd

### 4.5 `zrobot_control` — 电机测试客户端节点

- **主类**：`RobStrideClientNode`（继承 `rclcpp::Node`）
- **职责**：定时（5 秒间隔）调用 `/rob_stride_control` 服务，将所有电机目标位置逐步递增，用于验证通信链路和电机响应
- **使用场景**：集成测试、电机运动功能验证

### 4.6 `zrobot_deploy` — 部署与 FSM 控制节点

本包包含四个类：

#### 4.6.1 `FSM` 抽象基类（`FSM.h` / `FSM.cpp`）

- **职责**：定义状态机公共接口，封装对 `zrobot_bridge` 的 ROS 2 服务调用
- **关键成员**：
  - `control_client_`：调用 `RobStrideMsgs` 服务
  - `get_positions_client_`：调用 `GetPositions` 服务
  - `current_motor_positions_[23]`：当前关节位置缓存
  - `current_state_`：FSM 状态枚举（`IDLE` / `FIX_STAND`）

#### 4.6.2 `FixStand`（`FixStand.h` / `FixStand.cpp`）

- **职责**：将 23 个关节从当前位置平滑插值过渡到机械零位（0 rad）
- **内部状态**：`INIT` → `MOVING` → `STANDING`
- **插值方式**：线性插值（Lerp），默认用时 3.0 秒

#### 4.6.3 `Locomotion`（`Locomotion.h` / `Locomotion.cpp`）

- **职责**：加载 ONNX 强化学习策略，以 100 Hz 频率执行推理，输出 12 维关节位置目标
- **观测向量维度**：47 维
- **动作向量维度**：12 维
- **推理线程**：独立线程 `inference_thread_`，与主控线程并发
- **传感器订阅**：`imu/data`（角速度、重力向量）、`cmd_vel`（速度指令）

#### 4.6.4 `main.cpp` — 交互式入口

- **职责**：创建 ROS 2 节点、响应键盘输入、切换并驱动 FSM
- **控制频率**：100 Hz（`rclcpp::Rate rate(100)`）
- **键盘指令**：
  - `F` / `f`：启动 FixStand 状态机
  - `S` / `s`：停止当前状态机
  - `Q` / `q`：退出程序

---

## 5. 函数名称与功能说明

### 5.1 ImuDriverNode 函数列表

| 函数名 | 返回值 | 参数 | 功能描述 |
|--------|--------|------|---------|
| `ImuDriverNode()` | — | `port_name`, `baud_rate`, `protocol` | 构造函数：初始化成员变量，创建话题发布者，启动驱动线程 |
| `~ImuDriverNode()` | — | — | 析构函数：停止线程，关闭串口 |
| `driverLoop()` | `void` | — | 驱动线程入口：依次调用 `openPort()` 和 `readData()` |
| `openPort()` | `void` | — | 打开串口并设置波特率、数据位、停止位、校验位 |
| `readData()` | `void` | — | 循环读取串口数据字节，调用 `handleSerialData()` 处理 |
| `rs485RequestData()` | `void` | — | RS485 协议下按顺序发送 Modbus 请求帧以轮询 4 类传感器数据 |
| `handleSerialData()` | `bool` | `uint8_t data` | 协议状态机：根据帧头、类型字节收集数据，校验后调用 `processData()` |
| `processData()` | `void` | — | 将原始 int16 数据换算为物理量（m/s²、rad/s、°）并调用欧拉角转四元数 |
| `publishData()` | `void` | — | 将处理后的数据填入 ROS 消息并发布到 `imu/data` 和 `imu/ImuDataWithRPY` |
| `hexToShort()` | `vector<int16_t>` | `hex`, `protocol` | 将字节数组按协议类型转换为有符号 16 位整数数组 |
| `checkSum()` | `bool` | `data`, `checksum` | 对接收到的字节数组求和取低 8 位，与校验字节比对 |
| `getQuaternionFromEuler()` | `vector<double>` | `roll`, `pitch`, `yaw` | 将欧拉角（RPY）转换为四元数 [x, y, z, w] |

### 5.2 RobStrideMotor 函数列表

| 函数名 | 返回值 | 参数 | 功能描述 |
|--------|--------|------|---------|
| `RobStrideMotor()` | — | `can_interface`, `master_id`, `motor_id`, `actuator_type` | 构造函数：记录参数并调用 `init_socket()` |
| `~RobStrideMotor()` | — | — | 析构函数：关闭 CAN socket |
| `init_socket()` | `void` | — | 创建 `AF_CAN` / `SOCK_RAW` socket，绑定到指定 CAN 接口，设置扩展帧过滤器 |
| `receive()` | `ReceiveResult` | `timeout_sec` | 阻塞读取一帧 CAN 数据，解析扩展 ID 中的通信类型、附加数据、主机 ID |
| `receive_status_frame()` | `void` | — | 读取状态反馈帧，更新 `position_`、`velocity_`、`torque_`、`temperature_` |
| `send_motion_command()` | `tuple<float,float,float,float>` | `torque`, `position_rad`, `velocity_rad_s`, `kp`, `kd` | 运控模式：将控制量编码为 16 位定点数，组装并发送 CAN 帧，返回位置/速度/力矩/温度 |
| `send_velocity_mode_command()` | `tuple<float,float,float,float>` | `velocity_rad_s` | 速度模式：设置速度参数，发送 CAN 帧 |
| `RobStrite_Motor_PosPP_control()` | `tuple<float,float,float,float>` | `Speed`, `Acceleration`, `Angle` | 位置模式 PP：设置位置、速度、加速度参数，发送控制帧 |
| `RobStrite_Motor_PosCSP_control()` | `tuple<float,float,float,float>` | `Speed`, `Angle` | 位置模式 CSP（循环同步）：设置位置及速度限制，发送控制帧 |
| `RobStrite_Motor_Current_control()` | `tuple<float,float,float,float>` | `IqCommand`, `IdCommand` | 电流模式：设置 Iq/Id，发送电流控制帧 |
| `RobStrite_Motor_Set_Zero_control()` | `void` | — | 零位模式：发送运控模式帧将当前位置设为零位 |
| `enable_motor()` | `tuple<float,float,float,float>` | — | 发送使能指令（通信类型 0x03），返回当前反馈 |
| `Disenable_Motor()` | `void` | `clear_error` | 发送失能指令（通信类型 0x04），可选清除错误 |
| `Set_ZeroPos()` | `void` | — | 发送机械零位设置指令（通信类型 0x06）|
| `read_initial_position()` | `float` | — | 使能电机后等待反馈，返回初始位置（rad）|
| `Get_RobStrite_Motor_parameter()` | `void` | `Index` | 发送参数读取请求（通信类型 0x11）|
| `Set_RobStrite_Motor_parameter()` | `void` | `Index`, `Value`, `Value_mode` | 发送参数写入请求（通信类型 0x12）|
| `float_to_uint()` | `uint16_t` | `x`, `x_min`, `x_max`, `bits` | 将物理值线性映射到 N 位无符号整数 |
| `uint_to_float()` | `float` | `x_int`, `x_min`, `x_max`, `bits` | 将 N 位无符号整数反映射为物理值 |
| `Byte_to_float()` | `float` | `bytedata[]` | 将 4 字节小端数组解释为 IEEE 754 浮点数 |
| `return_data_pvtt()` | `tuple<float,float,float,float>` | — | 返回并打印上次接收的位置/速度/力矩/温度 |

### 5.3 MotorControllerNode 函数列表

| 函数名 | 返回值 | 参数 | 功能描述 |
|--------|--------|------|---------|
| `MotorControllerNode()` | — | — | 构造函数：从参数服务器读取配置，调用 `initialize_motors()`，注册 3 个服务 |
| `~MotorControllerNode()` | — | — | 析构函数：资源释放 |
| `initialize_motors()` | `void` | — | 遍历 23 个电机，创建 `RobStrideMotor` 对象并调用 `enable_motor()` |
| `handle_rob_stride_service()` | `void` | `request`, `response` | 运控服务回调：遍历 23 个电机调用 `send_motion_command()`，填充响应数组 |
| `handle_set_zeros_service()` | `void` | `request`, `response` | 零位服务回调：遍历 23 个电机调用 `Set_ZeroPos()`|
| `handle_get_positions_service()` | `void` | `request`, `response` | 位置查询服务回调：遍历 23 个电机调用 `enable_motor()` 获取反馈位置 |

### 5.4 FSM 基类函数列表

| 函数名 | 返回值 | 参数 | 功能描述 |
|--------|--------|------|---------|
| `FSM()` | — | `node` | 构造函数：保存节点指针，创建两个服务客户端 |
| `initialize()` | `virtual void` | — | 虚函数：将状态置为 `IDLE`，子类可扩展 |
| `run()` | `pure virtual void` | — | 纯虚函数：每控制周期调用一次，子类实现具体逻辑 |
| `exit()` | `virtual void` | — | 虚函数：将状态置为 `IDLE`，子类可在此处释放资源 |
| `getState()` | `FSMState` | — | 返回当前 FSM 状态枚举值 |
| `sendMotorPositions()` | `bool` | `positions[23]` | 同步调用 `RobStrideMsgs` 服务，发送目标位置，等待反馈 |
| `getMotorFeedback()` | `bool` | `positions`, `velocities`, `torques`, `temperatures` | 调用服务获取全部反馈数组（位置/速度/力矩/温度）|
| `getCurrentPositions()` | `bool` | `positions[23]` | 调用 `GetPositions` 服务获取当前各电机位置 |

### 5.5 FixStand 函数列表

| 函数名 | 返回值 | 参数 | 功能描述 |
|--------|--------|------|---------|
| `FixStand()` | — | `node` | 构造函数：初始化内部状态、目标/初始位置数组、插值参数 |
| `initialize()` | `void` | — | 调用父类初始化；进入 `INIT` 状态；设置插值总时间（默认 3.0 s）|
| `run()` | `void` | — | 每帧调用：`INIT` 阶段读初始位置，`MOVING` 阶段按插值系数发送中间位置，`STANDING` 阶段持续保持零位 |
| `exit()` | `void` | — | 调用父类 exit，重置内部状态标志 |
| `lerp()` | `float` | `start`, `end`, `t` | 线性插值辅助函数：`start + t * (end - start)`，`t ∈ [0,1]` |

### 5.6 Locomotion 函数列表

| 函数名 | 返回值 | 参数 | 功能描述 |
|--------|--------|------|---------|
| `Locomotion()` | — | `node` | 构造函数：初始化 ONNX 环境，清零 Eigen 向量 |
| `initialize()` | `void` | — | 读取 ROS 参数，订阅 IMU 和速度话题，加载 ONNX 模型，启动推理线程 |
| `run()` | `void` | — | 主控线程每帧调用：从 `act_scaled_` 取动作（互斥保护），发送到电机 |
| `exit()` | `void` | — | 停止推理线程（`thread_running_=false`），join 线程，调用父类 exit |
| `loadPolicy()` | `void` | `model_path` | 初始化 ONNX Runtime Session，提取输入输出名称 |
| `initializeParameters()` | `void` | — | 从 ROS 参数服务器读取 dt、phase_period、default_angles、Kp/Kd、缩放系数 |
| `inferenceLoop()` | `void` | — | 推理线程主循环：收集观测→归一化→ONNX 推理→反归一化→写入共享缓冲区 |
| `collectObservations()` | `void` | — | 将角速度、重力向量、速度指令、关节位置、关节速度、上次动作、相位信号填入 `obs_current_` |
| `runInference()` | `void` | — | 构造 ONNX 输入张量，调用 `ort_session_->Run()`，将输出写入 `act_scaled_` |
| `imuCallback()` | `void` | `sensor_msgs/Imu` | IMU 话题回调：提取角速度，利用 tf2 将四元数转换为重力向量，加锁更新共享数据 |
| `cmdCallback()` | `void` | `geometry_msgs/Twist` | 速度指令回调：加锁更新 `current_command_`（vx, vy, wz）|

---

## 6. 算法设计

### 6.1 IMU 数据解析算法

IMU 传感器通过串口以固定格式发送数据帧，系统支持三种协议，以 TTL 协议为例：

**帧格式**：

```
[0x55][类型][D0][D1][D2][D3][D4][D5][D6][D7][校验和]
  头    1B    8字节数据                            1B
```

**解析过程**：

1. 逐字节读取，等待帧头 `0x55`；
2. 接收类型字节（`0x51`~`0x54`），映射到加速度/角速度/姿态角/磁力计；
3. 接收 8 字节数据（TTL）或 6 字节数据（CAN）；
4. 校验和验证：将帧内所有字节相加取低 8 位，与最后的校验字节比较；
5. 每 2 个字节拼为 1 个 `int16_t`，得到原始传感器读数；
6. 物理量转换：

```
加速度：a_x = raw_ax / 32768.0 × 16 × 9.8   (m/s²)
角速度：ω_x = raw_gx / 32768.0 × 2000       (°/s) → 转 rad/s
姿态角：φ   = raw_roll / 32768.0 × 180       (°)   → 转 rad
```

### 6.2 欧拉角转四元数算法

采用 ZYX（Yaw-Pitch-Roll）旋转顺序：

```
cy = cos(yaw/2),   sy = sin(yaw/2)
cp = cos(pitch/2), sp = sin(pitch/2)
cr = cos(roll/2),  sr = sin(roll/2)

q.x = sr·cp·cy − cr·sp·sy
q.y = cr·sp·cy + sr·cp·sy
q.z = cr·cp·sy − sr·sp·cy
q.w = cr·cp·cy + sr·sp·sy
```

该四元数满足单位性约束 `‖q‖ = 1`，直接填入 `sensor_msgs/Imu.orientation` 字段。

### 6.3 CAN 扩展帧 ID 编解码算法

RobStride 电机使用 CAN 扩展帧（29 位 ID），自定义编码：

**发送端编码**：

```
can_id = (communication_type << 24)
       | (extra_data          << 8)
       | motor_id
can_id |= CAN_EFF_FLAG          // 置扩展帧标志位
```

**接收端解码**：

```
raw_id             = frame.can_id & CAN_EFF_MASK   // 去除标志位
communication_type = (raw_id >> 24) & 0x1F         // bits[28:24]
extra_data         = (raw_id >>  8) & 0xFFFF        // bits[23:8]
host_id            = (raw_id      ) & 0xFF           // bits[7:0]
error_code         = (raw_id >> 16) & 0x3F          // bits[21:16]
pattern            = (raw_id >> 22) & 0x03          // bits[23:22]
```

**运控模式数据帧**（8 字节数据域）：

```
data[1:0] = float_to_uint(position, -12.566, 12.566, 16)  // 4π rad 范围
data[3:2] = float_to_uint(velocity, -20,     20,     16)
data[4]   = float_to_uint(kp,        0,      500,     8)
data[5]   = float_to_uint(kd,        0,       5,      8)
data[7:6] = float_to_uint(torque,  -17,      17,     16)
```

### 6.4 浮点数与 16 位定点数互转算法

用于将物理量压缩为 CAN 帧内有限位宽整数：

**浮点 → 无符号整数**：

```
span   = x_max - x_min
offset = x - x_min
result = round( offset × (2^bits - 1) / span )
```

**无符号整数 → 浮点**：

```
span   = x_max - x_min
result = x_int × span / (2^bits - 1) + x_min
```

量化误差：当 bits=16 时，分辨率 ≈ `span / 65535`。

### 6.5 线性插值（Lerp）站立算法

FixStand 采用基于时间的线性插值，将关节从初始位置平滑移动到零位：

```
已知：
  q_init[i]  ← 初始化时读取的电机实际位置
  q_target[i] = 0.0  （机械零位）
  T           = interpolation_time_  （插值总时长，默认 3.0 s）

每帧：
  Δt         = 当前时刻 − 上次时刻
  elapsed    += Δt
  t           = clamp(elapsed / T, 0.0, 1.0)

  q_cmd[i]   = q_init[i] + t × (q_target[i] − q_init[i])
             = lerp(q_init[i], q_target[i], t)

当 t ≥ 1.0 时，切换到 STANDING 状态，持续发送 q_target
```

### 6.6 强化学习推理（Locomotion）算法

系统采用预训练强化学习策略（ONNX 格式），实现基于学习的步态控制：

**观测向量构成（47 维）**：

```
obs[0:3]   = ω_body           ← IMU 角速度（body frame，rad/s）× ang_vel_scale
obs[3:6]   = g_body           ← 重力向量在 body frame 中的投影
obs[6:9]   = v_cmd            ← 速度指令（vx, vy, wz）
obs[9:21]  = q_joint − q_default  ← 12 个关节位置偏差 × dof_pos_scale
obs[21:33] = dq_joint         ← 12 个关节速度 × dof_vel_scale
obs[33:45] = a_prev           ← 上一时刻 12 维动作
obs[45:47] = [sin(φ), cos(φ)]  ← 步态相位信号，φ = 2π × counter × dt / T_phase
```

**推理流程**：

```
1. 归一化观测：obs_scaled = (obs_current - obs_mean) × obs_scales
2. 构建输入张量：shape=[1, 47]
3. ONNX 推理：act_raw = session.Run(obs_scaled)
4. 反归一化动作：act_scaled = act_raw × act_scales + act_mean
5. 写入共享缓冲区（互斥保护）
6. counter++
```

**PD 控制**：推理输出为目标关节位置，由 `motor_controller_node` 中电机 Kp/Kd 参数实现 PD 跟踪。

---

## 7. 各功能流程图

### 7.1 系统启动流程

```
开始
  │
  ▼
启动 motor_controller_node
  │  (从 motor_config.yaml 读取参数)
  │  (初始化 23 个 RobStrideMotor)
  │  (注册 3 个 ROS 2 服务)
  ▼
启动 imu_data_node
  │  (打开串口 /dev/imu_usb)
  │  (启动驱动线程)
  │  (创建话题发布者)
  ▼
启动 zrobot_deploy (main)
  │  (初始化 ROS 2 节点 zrobot_fsm_controller)
  │  (显示键盘菜单)
  ▼
进入主控制循环 (100 Hz)
  │
  ├─ 监听键盘输入
  ├─ 运行当前 FSM
  └─ rclcpp::spin_some()
```

### 7.2 IMU 数据采集与发布流程

```
驱动线程启动
  │
  ▼
openPort() → 串口初始化
  │
  ▼
readData() 循环
  │
  ├─ [RS485] rs485RequestData() → 发送 Modbus 请求
  │
  ├─ serial_port_.ReadByte(byte)
  │
  ▼
handleSerialData(byte)
  │
  ├─ key_==0: 等待帧头 0x55
  │
  ├─ key_==1: 读取数据类型 (0x51/0x52/0x53/0x54)
  │
  ├─ key_==2~9: 收集数据字节存入 buffer_
  │
  └─ key_==10 (TTL) 或 key_==8 (CAN):
       ├─ checkSum() 校验
       ├─ hexToShort() 转换
       └─ processData() 物理量转换
              │
              ▼
           publishData()
              ├─ 发布 imu/data (sensor_msgs/Imu)
              └─ 发布 imu/ImuDataWithRPY (imu_msg/ImuData)
```

### 7.3 电机运控服务处理流程

```
客户端发起请求 (positions[23])
  │
  ▼
MotorControllerNode::handle_rob_stride_service()
  │
  ├─ 加锁 motors_mutex_
  │
  ├─ for i = 0 to 22:
  │    │
  │    ├─ motors_[i]->send_motion_command(
  │    │     torque=0, position=request[i],
  │    │     velocity=0, kp=motor_kps_[i], kd=motor_kds_[i])
  │    │
  │    │   内部流程：
  │    │   ├─ float_to_uint(position, -4π, 4π, 16)
  │    │   ├─ float_to_uint(velocity, -20, 20, 16)
  │    │   ├─ float_to_uint(kp, 0, 500, 8)
  │    │   ├─ float_to_uint(kd, 0, 5, 8)
  │    │   ├─ float_to_uint(torque, -17, 17, 16)
  │    │   ├─ 组装 CAN 扩展帧 (ID = 0x01<<24 | 0<<8 | motor_id)
  │    │   ├─ send(socket_fd, frame, ...)
  │    │   ├─ receive() 等待反馈帧
  │    │   └─ uint_to_float() 解码反馈数据
  │    │
  │    └─ 填充 response.feedback_positions/velocities/torques/temperatures
  │
  ├─ 解锁
  │
  └─ response.success = true → 返回响应
```

### 7.4 电机零位标定流程

```
客户端发起 SetZeros 请求
  │
  ▼
handle_set_zeros_service()
  │
  ├─ 加锁 motors_mutex_
  │
  ├─ for i = 0 to 22:
  │    motors_[i]->Set_ZeroPos()
  │    │
  │    └─ 发送 CAN 帧：
  │         can_id = (0x06 << 24) | (1 << 8) | motor_id
  │         data = [0x00 × 8]
  │
  ├─ 解锁
  │
  └─ response.success = true → 返回响应
```

### 7.5 FixStand 状态机流程

```
FixStand::initialize()
  │
  ├─ 内部状态 = INIT
  ├─ interpolation_time_ = 3.0 s
  └─ is_initialized_ = false

FixStand::run() (每帧，100 Hz)
  │
  ├─ [INIT 状态]
  │    ├─ getCurrentPositions(initial_positions_) 读取初始位置
  │    ├─ target_positions_ = {0, ..., 0}
  │    ├─ start_time_ = now()
  │    └─ 切换到 MOVING 状态
  │
  ├─ [MOVING 状态]
  │    ├─ Δt = now() - last_time_
  │    ├─ elapsed_time_ += Δt
  │    ├─ t = clamp(elapsed_time_ / interpolation_time_, 0, 1)
  │    ├─ for i: q_cmd[i] = lerp(initial_positions_[i], 0, t)
  │    ├─ sendMotorPositions(q_cmd)
  │    └─ [t >= 1.0] 切换到 STANDING 状态
  │
  └─ [STANDING 状态]
       └─ sendMotorPositions({0, ..., 0})  持续保持零位
```

### 7.6 Locomotion 推理控制流程

```
Locomotion::initialize()
  │
  ├─ initializeParameters()   读取 ROS 参数
  ├─ 订阅 imu/data 和 cmd_vel
  ├─ loadPolicy(model_path)   加载 ONNX 模型
  └─ 启动推理线程 inferenceLoop()

推理线程 inferenceLoop() (100 Hz):
  │
  ├─ 循环:
  │    ├─ collectObservations()
  │    │    ├─ 加锁读取 IMU 数据 (角速度, 重力向量)
  │    │    ├─ 读取速度指令
  │    │    ├─ 读取关节位置/速度
  │    │    ├─ 读取上次动作
  │    │    └─ 计算相位信号 sin/cos(2π×counter×dt/T)
  │    │
  │    ├─ runInference()
  │    │    ├─ obs_scaled = (obs_current - obs_mean) * obs_scales
  │    │    ├─ 构建 Ort::Value 输入张量 [1×47]
  │    │    ├─ session->Run(input) → output
  │    │    └─ act_scaled = output * act_scales + act_mean
  │    │
  │    ├─ 加锁写入 act_temp_
  │    └─ counter++; sleep(dt_)

Locomotion::run() (主控线程，100 Hz):
  │
  ├─ 加锁读取 act_temp_
  ├─ positions[0:12] = act_temp_
  └─ sendMotorPositions(positions)
```

### 7.7 FSM 主程序交互流程

```
程序启动
  │
  ▼
rclcpp::init()
创建节点 zrobot_fsm_controller
显示菜单
  │
  ▼
主循环 (100 Hz):
  │
  ├─ kbhit() 检测键盘
  │    ├─ 'F': 停止当前 FSM → 创建 FixStand → initialize()
  │    ├─ 'S': 停止当前 FSM
  │    └─ 'Q': running = false
  │
  ├─ [current_fsm != null] current_fsm->run()
  │
  ├─ rclcpp::spin_some(node)
  │
  └─ rate.sleep()

running == false:
  ├─ current_fsm->exit()
  ├─ rclcpp::shutdown()
  └─ 退出
```

---

## 8. 逻辑框图

### 8.1 整体逻辑框图

```
┌─────────────────────────────────────────────────────────────────────┐
│                         外部输入                                     │
│   键盘指令 (F/S/Q)    速度指令 cmd_vel    IMU 传感器数据             │
└──────────┬──────────────────┬────────────────────┬──────────────────┘
           │                  │                    │
           ▼                  ▼                    ▼
┌──────────────────┐ ┌─────────────────┐  ┌───────────────────────┐
│  main.cpp        │ │  Locomotion FSM │  │  imu_data_node        │
│  FSM 调度器      │ │  (ONNX 推理)    │◄─│  串口解析 + ROS 发布  │
│  键盘事件处理    │ └────────┬────────┘  └───────────────────────┘
└──────────┬───────┘          │
           │                  │ 位置指令 (12维)
           │ FSM 切换         │
           ▼                  ▼
┌──────────────────┐ ┌─────────────────────────────────────────────┐
│  FixStand FSM    │ │  FSM 基类 (sendMotorPositions)              │
│  线性插值站立    │ │  ROS Service Client → rob_stride_control    │
└──────────┬───────┘ └──────────────────────┬────────────────────┘
           │ 位置指令 (23维)                 │
           └─────────────────────────────────┘
                                             │ ROS Service 请求
                                             ▼
┌────────────────────────────────────────────────────────────────────┐
│  MotorControllerNode (zrobot_bridge)                                │
│  ┌──────┐ ┌──────┐ ┌──────┐  ...  ┌──────┐                        │
│  │Motor0│ │Motor1│ │Motor2│  ...  │Motor22│                        │
│  └──┬───┘ └──┬───┘ └──┬───┘       └──┬───┘                        │
└─────┼────────┼─────────┼─────────────┼──────────────────────────┘
      │CAN     │CAN       │CAN          │CAN
      ▼        ▼          ▼             ▼
  can10      can10      can10        can13
  (0-5号)   (0-5号)    (0-5号)      (17-22号)
      │
      ▼
  RobStride 电机硬件 (×23)
```

### 8.2 IMU 驱动逻辑框图

```
                  ┌──────────────────────────────┐
                  │       ImuDriverNode          │
                  │                              │
  /dev/imu_usb ──►│  driver_thread_              │
  (串口硬件)      │  ┌────────────────────────┐  │
                  │  │ openPort()             │  │
                  │  │  设置波特率/数据位     │  │
                  │  └──────────┬─────────────┘  │
                  │             │                 │
                  │  ┌──────────▼─────────────┐  │
                  │  │ readData()             │  │
                  │  │  循环读字节            │  │
                  │  └──────────┬─────────────┘  │
                  │             │                 │
                  │  ┌──────────▼─────────────┐  │
                  │  │ handleSerialData()     │  │
                  │  │  协议状态机            │  │
                  │  │  TTL/CAN/RS485 解析   │  │
                  │  └──────────┬─────────────┘  │
                  │             │                 │
                  │  ┌──────────▼─────────────┐  │
                  │  │ processData()          │  │
                  │  │  原始值 → 物理量       │  │
                  │  │  欧拉角 → 四元数       │  │
                  │  └──────────┬─────────────┘  │
                  │             │                 │
                  │  ┌──────────▼─────────────┐  │
                  │  │ publishData()          │  │
                  │  └──────────┬─────────────┘  │
                  └─────────────┼────────────────┘
                                │
                   ┌────────────┴────────────┐
                   ▼                         ▼
            imu/data                 imu/ImuDataWithRPY
         (sensor_msgs/Imu)           (imu_msg/ImuData)
```

### 8.3 电机控制逻辑框图

```
┌─────────────────────────────────────────────────────────────────────┐
│                    MotorControllerNode                               │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                  服务接口层                                  │   │
│  │  rob_stride_control ──► handle_rob_stride_service()         │   │
│  │  set_zeros          ──► handle_set_zeros_service()          │   │
│  │  get_positions      ──► handle_get_positions_service()      │   │
│  └────────────────────────────┬────────────────────────────────┘   │
│                               │ motors_mutex_ 保护                   │
│  ┌────────────────────────────▼────────────────────────────────┐   │
│  │               电机管理层 (23个电机)                          │   │
│  │  motors_[0]   motors_[1]   ...   motors_[11]  ...motors_[22]│   │
│  └────────────────────────────┬────────────────────────────────┘   │
│                               │                                      │
│  ┌────────────────────────────▼────────────────────────────────┐   │
│  │                  RobStrideMotor 层                           │   │
│  │                                                              │   │
│  │  init_socket() ─────────────► AF_CAN SOCK_RAW socket        │   │
│  │  send_motion_command()                                       │   │
│  │    float_to_uint() ──────────► 打包 CAN 扩展帧              │   │
│  │    send(socket_fd, frame) ───► CAN 总线发送                 │   │
│  │    receive() ◄───────────────── CAN 总线接收                │   │
│  │    uint_to_float() ◄──────────── 解码反馈                   │   │
│  └──────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
         │ can10         │ can11       │ can12      │ can13
         ▼               ▼             ▼            ▼
    电机 0~5          电机 6~12     电机 13~16  电机 17~22
```

### 8.4 FSM 状态转换逻辑框图

```
                   键盘按下 'F'
                        │
    ┌───────────────────▼───────────────────┐
    │              IDLE (初始)               │
    │  current_fsm = nullptr                 │
    └──────────────────┬────────────────────┘
                       │ 按下 'F'
                       ▼
    ┌──────────────────────────────────────────┐
    │           FixStand 状态机                 │
    │  ┌────────┐                               │
    │  │  INIT  │ ← initialize()                │
    │  └───┬────┘                               │
    │      │ 读取初始位置成功                    │
    │      ▼                                    │
    │  ┌────────┐                               │
    │  │MOVING  │ t 从 0 增至 1 (3秒)           │
    │  └───┬────┘                               │
    │      │ t >= 1.0                            │
    │      ▼                                    │
    │  ┌──────────┐                             │
    │  │STANDING  │ 保持零位                    │
    │  └──────────┘                             │
    └──────────────────────────────────────────┘
              │ 按下 'S' 或 'Q'
              ▼
         exit() → IDLE
```

### 8.5 Locomotion 强化学习控制逻辑框图

```
┌──────────────────────────────────────────────────────────────────┐
│                      Locomotion 状态机                            │
│                                                                   │
│  ┌──────────────┐    ┌────────────────────────────────────────┐  │
│  │  主控线程     │    │           推理线程 (100 Hz)             │  │
│  │  run()       │    │  collectObservations()                  │  │
│  │              │    │    ├─ 加锁读 IMU 角速度/重力向量        │  │
│  │  加锁读      │    │    ├─ 读速度指令 cmd_vel                │  │
│  │  act_temp_   │    │    ├─ 读关节位置/速度                   │  │
│  │              │    │    └─ 计算相位 sin/cos                  │  │
│  │  发送到       │    │                                         │  │
│  │  23个电机    │    │  runInference()                         │  │
│  └──────┬───────┘    │    ├─ 归一化观测 (47维)                │  │
│         │            │    ├─ ONNX Session.Run()               │  │
│  ←─── action_mutex_ ─┤    └─ 反归一化动作 (12维)              │  │
│                       │                                         │  │
│                       │  加锁写 act_scaled_                    │  │
│                       └────────────────────────────────────────┘  │
│                                                                   │
│  外部输入:                                                         │
│    imu/data ──► imuCallback() ─────────────────► 角速度/重力    │  │
│    cmd_vel  ──► cmdCallback() ─────────────────► 速度指令        │  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 9. 运行设计

### 9.1 节点启动顺序

建议按以下顺序启动节点：

```
步骤 1：配置 CAN 接口（需 root 权限）
  bash src/zrobot_bridge/scripts/setup_can_interfaces.sh

步骤 2：启动电机控制桥接节点
  ros2 launch zrobot_bridge motor_controller.launch.py

步骤 3：启动 IMU 驱动节点
  ros2 run imu_data_node imu_data_node

步骤 4：启动控制决策节点
  ros2 run zrobot_deploy zrobot_deploy_main
```

`motor_controller_node` 必须在 FSM 节点之前启动，以确保服务可用。

### 9.2 控制频率与实时性

| 组件 | 运行频率 | 时间约束 |
|------|---------|---------|
| `imu_data_node` 串口读取 | ~50 Hz | 串口硬件决定 |
| `MotorControllerNode` 服务响应 | 按需，上限约 100 Hz | 每次 23 帧 CAN 通信 ≈ 5–20 ms |
| `main.cpp` 主循环 | 100 Hz | `rclcpp::Rate(100)` |
| `Locomotion` 推理线程 | 100 Hz | ONNX 推理 + 观测采集 < 10 ms |
| `FixStand::run()` | 100 Hz | 插值计算 + 服务调用 |
| `rob_stride_client_node` | 0.2 Hz（5 s 定时）| 测试用，无实时要求 |

### 9.3 线程模型

```
进程 motor_controller_node:
  ├─ 主线程：rclcpp executor (处理服务请求)
  └─ （无额外线程）

进程 imu_data_node:
  ├─ 主线程：rclcpp executor (处理 ROS 回调)
  └─ driver_thread_：串口读取 + 数据解析 + publishData()

进程 zrobot_deploy (main):
  ├─ 主线程：主控制循环 + rclcpp::spin_some()
  └─ inference_thread_（仅 Locomotion 模式）：ONNX 推理

共享数据保护：
  - motors_mutex_：MotorControllerNode 中保护电机数组
  - sensor_data_mutex_：Locomotion 中保护 IMU 读数
  - action_mutex_：Locomotion 中保护推理输出
```

### 9.4 错误处理与容错机制

| 错误类型 | 处理策略 |
|---------|---------|
| 串口打开失败 | `RCLCPP_FATAL` 输出，抛出 `OpenFailed` 异常，节点退出 |
| CAN socket 初始化失败 | 打印错误，socket_fd 保持 -1，后续操作返回 nullopt |
| CAN 接收超时 | `receive()` 返回 `std::nullopt`，上层检查后跳过该电机 |
| IMU 帧校验失败 | 丢弃当前帧，重置解析状态机，等待下一帧头 |
| 服务调用超时（100 ms）| 返回 false，FSM 上层打印 ERROR 日志，跳过本帧 |
| ONNX 模型加载失败 | 抛出 `Ort::Exception`，`initialize()` 异常退出 |
| 电机错误码非零 | `error_code` 字段由 `receive()` 更新，上层可查询 |

### 9.5 参数配置说明

#### `motor_config.yaml` 关键参数

| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `master_id` | int | `0xFD` | CAN 主机 ID |
| `motor_can_ids` | int[23] | 0~22 | 各电机 CAN ID |
| `motor_types` | int[23] | 见配置文件 | 电机类型（0~6，对应 ROBSTRIDE_00~06）|
| `motor_can_interfaces` | string[23] | can10/11/12/13 | 各电机所连 CAN 接口 |
| `motor_kps` | float[23] | 1.0 | 各电机位置环 P 增益 |
| `motor_kds` | float[23] | 0.5 | 各电机速度环 D 增益 |

#### Locomotion ROS 参数

| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `onnx_model_path` | string | `/root/codes/.../policy.onnx` | 策略模型路径 |
| `control_dt` | double | `0.01` | 推理控制周期（秒）|
| `phase_period` | double | `0.8` | 步态相位周期（秒）|
| `default_angles` | double[12] | 全零 | 关节默认角度偏置 |
| `kps` | double[12] | 30.0 | 关节刚度 Kp |
| `kds` | double[12] | 1.0 | 关节阻尼 Kd |
| `ang_vel_scale` | double | `1.0` | 角速度观测缩放 |
| `dof_pos_scale` | double | `1.0` | 关节位置观测缩放 |
| `dof_vel_scale` | double | `1.0` | 关节速度观测缩放 |
| `action_scale` | double | `1.0` | 动作输出缩放 |

---

## 10. 附录

### 10.1 电机类型参数表

| 类型枚举 | 最大位置 (rad) | 最大速度 (rad/s) | 最大力矩 (N·m) | 默认 Kp | 默认 Kd |
|---------|--------------|----------------|--------------|---------|---------|
| ROBSTRIDE_00 | ±4π | 50 | 17 | 500 | 5 |
| ROBSTRIDE_01 | ±4π | 44 | 17 | 500 | 5 |
| ROBSTRIDE_02 | ±4π | 44 | 17 | 500 | 5 |
| ROBSTRIDE_03 | ±4π | 50 | 60 | 5000 | 100 |
| ROBSTRIDE_04 | ±4π | 15 | 120 | 5000 | 100 |
| ROBSTRIDE_05 | ±4π | 33 | 17 | 500 | 5 |
| ROBSTRIDE_06 | ±4π | 20 | 60 | 5000 | 100 |

### 10.2 CAN 通信类型定义表

| 通信类型 ID | 宏定义 | 功能 |
|------------|--------|------|
| `0x00` | `Communication_Type_Get_ID` | 获取设备 ID 及 MCU 唯一标识符 |
| `0x01` | `Communication_Type_MotionControl` | 运控模式，发送位置/速度/力矩指令 |
| `0x02` | `Communication_Type_MotorRequest` | 请求电机状态反馈 |
| `0x03` | `Communication_Type_MotorEnable` | 使能电机 |
| `0x04` | `Communication_Type_MotorStop` | 停止电机 |
| `0x06` | `Communication_Type_SetPosZero` | 设置机械零位 |
| `0x07` | `Communication_Type_Can_ID` | 更改电机 CAN ID |
| `0x11` | `Communication_Type_GetSingleParameter` | 读取单个参数 |
| `0x12` | `Communication_Type_SetSingleParameter` | 写入单个参数 |
| `0x15` | `Communication_Type_ErrorFeedback` | 故障反馈帧 |

### 10.3 电机配置映射表

| 电机索引 | CAN ID | 类型 | CAN 接口 | 说明（参考）|
|---------|--------|------|---------|------------|
| 0 | 0 | ROBSTRIDE_02 | can10 | — |
| 1 | 1 | ROBSTRIDE_02 | can10 | — |
| 2 | 2 | ROBSTRIDE_04 | can10 | — |
| 3 | 3 | ROBSTRIDE_04 | can10 | — |
| 4 | 4 | ROBSTRIDE_03 | can10 | — |
| 5 | 5 | ROBSTRIDE_03 | can10 | — |
| 6 | 6 | ROBSTRIDE_02 | can11 | — |
| 7 | 7 | ROBSTRIDE_02 | can11 | — |
| 8 | 8 | ROBSTRIDE_04 | can11 | — |
| 9 | 9 | ROBSTRIDE_04 | can11 | — |
| 10 | 10 | ROBSTRIDE_03 | can11 | — |
| 11 | 11 | ROBSTRIDE_03 | can11 | — |
| 12 | 12 | ROBSTRIDE_02 | can11 | — |
| 13 | 13 | ROBSTRIDE_02 | can12 | — |
| 14 | 14 | ROBSTRIDE_02 | can12 | — |
| 15 | 15 | ROBSTRIDE_02 | can12 | — |
| 16 | 16 | ROBSTRIDE_02 | can12 | — |
| 17 | 17 | ROBSTRIDE_02 | can13 | — |
| 18 | 18 | ROBSTRIDE_02 | can13 | — |
| 19 | 19 | ROBSTRIDE_02 | can13 | — |
| 20 | 20 | ROBSTRIDE_02 | can13 | — |
| 21 | 21 | ROBSTRIDE_02 | can13 | — |
| 22 | 22 | ROBSTRIDE_02 | can13 | — |

---

*本文档依据 `src/` 目录下全部源代码整理，如有代码更新请同步更新本文档。*
